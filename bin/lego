#!/bin/bash -eu

usage() {
  lego-help
  exit 1
}

main() {
  if [ $# -eq 0  ]; then
    usage
  fi

  cmd="lego-$1"

  if command -v "$cmd" > /dev/null; then
    shift
    exec "$cmd" "$@"
  else
    echo "unknown command: $1"
    usage
  fi
}

main "$@"
